# Copy this file to .env and fill in your values.
OPENAI_API_KEY=sk-...
# Optional: override the default model (gpt-4o)
OPENAI_CHAT_MODEL=gpt-4o
# GPT 5.x reasoning models can spend low budgets on reasoning tokens only.
OPENAI_MAX_COMPLETION_TOKENS=512
